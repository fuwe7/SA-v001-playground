# Зависимости для анализа броска (Этап 0). Работает на обычном CPU, GPU не требуется.
# Версии зафиксированы: mediapipe 0.10.21 — последняя с классическим API mp.solutions,
# который использует core/detector.py. Она требует numpy<2, поэтому opencv тоже совместимой версии.
opencv-python==4.9.0.80
mediapipe==0.10.21
numpy<2

# Этап 1.5: простой веб-экран «видео → отчёт».
Flask==3.0.3
